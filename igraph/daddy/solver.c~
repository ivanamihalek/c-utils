#include "igraph_daddy.h"

void *  construct_graph(char *infile){

    FILE * infile  =  efopen(argv[1], "r") ;
    igraph_t graph;
    igraph_read_graph_edgelist (&graph, infile,  0, IGRAPH_UNDIRECTED);
    fclose(infile);

    return &graph
}
